apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: TestOrg
      defaultBucket: TestBucket
      tlsSkipVerify: true
    secureJsonData:
      token: abc123!?-
